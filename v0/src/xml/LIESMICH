Um die Anzhal der Datenbank abfragen ein zu schränken.
Ist hier eine zweite Variante der Autovervollständigung.
Dabei ist der Dateiname Identisch mit dem Tabellenfeldnamen
auf das die Autovervollständigung angewendet werden soll.
Als Suffix folgt die Landesprache.
Der Dateiname muss in Kleinschreibung sein!

  "{db_feldname}_{locale}.xml"

Die Struktur ist wie folgt aufgebaut:

  <?xml version="1.0" encoding="utf-8"?>
  <itemlist>
    <item>TEXT</item>
  </itemlist>

Die Datei wird unter "{QStandardPaths::DataLocation}/xml/" gespeichert und gesucht.
